@model List<PhotonServer>
<b>Services :</b>
@if (Model.Count > 0)
{
    foreach (var photonServer in Model)
    {
    <span class="bold">@photonServer.Name</span> @:--
    }
}
else
{
    <span class="italic">none</span>
}